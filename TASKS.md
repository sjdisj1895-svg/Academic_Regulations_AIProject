# TASKS: AI 기반 대학·산학협력단 규정집 통합 검색 시스템 — 단계별 구현 계획

- **기준 문서**: `PRD.md` (v1.0)
- **작성일**: 2026-08-31
- **원칙**: 한 번에 전부 만들지 않고, **5개의 작업(Task)** 으로 나누어 앞 단계의 결과물을 바탕으로 다음 단계를 진행한다.

---

## 개발 대상 원본 사이트 (데이터 출처)

| 구분 | 사이트 | URL | 특징 |
|---|---|---|---|
| 대학 | 학칙/규정/지침 페이지 | https://www.gnu.ac.kr/main/cm/cntnts/cntntsView.do?mi=1255&cntntsId=1214 | '학칙 / 규정 / 지침' 탭으로 구분된 규정 목록 (담당: 총무과, 055-772-0334) |
| 산학협력단 | 규정집 게시판 | https://www.gnu.ac.kr/research/na/ntt/selectNttList.do?mi=7117&bbsId=2456 | 기준일자별 '산학협력단 규정집' 파일이 첨부된 게시판 (담당: 산학연구과) |

> ⚠️ 두 사이트는 **구조가 서로 다릅니다.** 대학은 규정별 목록 형태, 산학협력단은 규정집 전체가 하나의 첨부파일(게시글) 형태이므로, 데이터 수집 방법을 각각 따로 만들어야 합니다.

---

## 전체 흐름 한눈에 보기

```
[Task 1] 규정 데이터 수집          →  두 사이트에서 규정 원문을 가져와 텍스트로 정리
[Task 2] 청킹 + 벡터DB 구축        →  규정을 '조·항' 단위로 잘게 나누어 AI가 검색할 수 있는 DB에 저장
[Task 3] 검색 엔진(API) 개발       →  키워드/자연어로 물어보면 관련 조항을 찾아주는 검색 기능 개발
[Task 4] 검색 화면(웹 UI) 개발     →  누구나 쓸 수 있는 검색창과 결과 화면 만들기
[Task 5] 품질 검증 및 운영 준비    →  검색 정확도 점검, 규정 개정 시 갱신 절차 마련
```

---

## Task 1. 규정 데이터 수집 및 텍스트 정리 (데이터 수집 파이프라인)

> **쉽게 말하면**: 흩어져 있는 대학 규정과 산학협력단 규정을 한곳에 모아, 컴퓨터가 읽을 수 있는 '깨끗한 글자 데이터'로 만드는 단계입니다. 요리로 치면 **장보기와 재료 손질**에 해당합니다.

### 구현 내용
1. **대학 규정 수집**: 대학 사이트(학칙/규정/지침 페이지)에서 규정 목록과 각 규정의 원문 파일(HWP/PDF 등)을 자동으로 내려받는 프로그램 작성
2. **산학협력단 규정 수집**: 산학협력단 규정집 게시판에서 **최신 기준일자 게시글**의 첨부파일(통합 규정집)을 내려받는 프로그램 작성
3. **텍스트 추출·정규화**: HWP/PDF 파일에서 글자를 추출하고, 깨진 글자·불필요한 서식 제거 등 정리 작업
4. **기본 정보표(메타데이터) 작성**: 규정마다 출처(대학/산학협력단), 카테고리(학칙/규정/지침), 규정명, 담당부서·연락처, 원본 페이지 URL을 정리한 목록 생성
5. 정리된 결과를 파일(예: JSON)로 저장하여 다음 단계에서 사용

### 관련 요구사항
- DR-01 (규정 원문 수집), DR-04 일부 (메타데이터), FR-06 일부 (카테고리 구분 체계)

### 완료 기준 (이 단계가 끝나면)
- ✅ 대학 + 산학협력단의 모든 규정이 텍스트 파일로 저장되어 있다
- ✅ 각 규정에 출처·카테고리·규정명·담당부서·원본 URL 정보가 붙어 있다

---

## Task 2. 조·항 단위 청킹 및 벡터DB 구축 (AI 검색의 심장)

> **쉽게 말하면**: Task 1에서 모은 규정 전문(全文)을 '제3조 제2항'처럼 **조항 단위로 잘게 자른 뒤**, AI가 "의미가 비슷한 글"을 찾을 수 있도록 특수한 데이터베이스(벡터DB)에 넣는 단계입니다. 책 전체를 통째로 두는 게 아니라, **문단마다 색인 카드를 만들어 도서관 서랍에 정리**하는 것과 같습니다.

### 구현 내용
1. **조·항 단위 청킹**: 규정 텍스트를 '제0조(제목) 제0항' 패턴으로 인식하여 조항 단위로 분할하는 프로그램 작성 (조문 구조가 특이한 규정 예외 처리 포함)
2. **청크 메타데이터 부여**: 잘라낸 조각(청크)마다 출처, 규정 구분, 규정명, 조·항 번호, 담당부서·연락처, 원본 URL을 붙임 (Task 1의 정보표 활용)
3. **임베딩 생성**: 각 청크의 문장을 AI 임베딩 모델로 '의미를 담은 숫자(벡터)'로 변환
4. **벡터DB 적재**: 변환된 벡터와 메타데이터를 벡터DB에 저장
5. **재실행 가능 구조**: 규정이 바뀌면 해당 규정만 다시 잘라서 다시 넣을 수 있도록 파이프라인을 반복 실행 가능하게 구성

### 관련 요구사항
- DR-02 (조·항 단위 청킹), DR-03 (임베딩·벡터DB), DR-04 (청크 메타데이터), NFR-05 (유지보수성)

### 완료 기준
- ✅ 벡터DB에 전체 규정이 조항 단위로 저장되어 있다
- ✅ 테스트 문장(예: "연구비")으로 벡터DB를 조회하면 의미가 관련된 조항들이 반환된다

---

## Task 3. 통합 검색 엔진(백엔드 API) 개발

> **쉽게 말하면**: 사용자가 검색어를 입력하면, Task 2에서 만든 벡터DB에서 **관련 조항을 찾아 정리된 형태로 돌려주는 '검색 두뇌'**를 만드는 단계입니다. 아직 화면은 없고, 뒤에서 일하는 검색 서버를 만드는 것입니다.

### 구현 내용
1. **하이브리드 검색 로직**: ① 의미 기반 검색(벡터 유사도) + ② 키워드 검색을 결합하여, "연구비"처럼 단어로 검색해도, "휴학하려면 어떻게 해?"처럼 문장으로 검색해도 정확한 조항을 찾도록 구현
2. **검색 API 개발**: 검색어를 받으면 결과를 돌려주는 웹 API 서버 개발
   - 결과 항목: 규정 구분, 규정명(연관 규정 **모두 표시**), 조항 번호(제0조 제0항), 본문 발췌, 담당부서·연락처, 원본 URL
3. **카테고리 필터**: 학칙/규정/지침, 대학/산학협력단 등 카테고리로 결과를 좁힐 수 있는 필터 기능
4. **미리보기용 API**: 특정 조항 선택 시 해당 조항(또는 규정 전문)을 돌려주는 상세 조회 API
5. **응답 속도 관리**: 검색 결과가 3초 이내에 반환되도록 성능 확인

### 관련 요구사항
- FR-01 (통합 검색), FR-02 (AI 하이브리드 검색), FR-03 (결과 상세 데이터), FR-06 (카테고리 필터), NFR-01 (응답 속도)

### 완료 기준
- ✅ API에 "연구비"를 보내면 PRD 예시처럼 [규정 구분 / 규정명 / 조항·본문 / 담당부서] 형태의 결과가 반환된다
- ✅ 카테고리 필터와 상세(미리보기) 조회가 동작한다

---

## Task 4. 통합 검색 웹 화면(프론트엔드 UI) 개발

> **쉽게 말하면**: 학생·교직원 누구나 쓸 수 있는 **실제 검색 화면(웹페이지)**을 만드는 단계입니다. 네이버 검색창처럼 검색어를 넣으면 결과 목록이 보기 좋게 나오고, 클릭하면 미리보기나 원본 사이트로 이동할 수 있게 합니다. (조회 형식은 연세대 규정집 사이트 벤치마킹)

### 구현 내용
1. **검색 메인 화면**: 단일 통합 검색창 + 카테고리 필터(학칙/규정/지침, 대학/산학협력단)
2. **검색 결과 목록 화면**: 결과마다 다음을 카드/표 형태로 표시
   - 규정 구분 · 규정명 · '제0조 제0항' 본문 발췌 · 담당부서(연락처)
   - 검색어와 일치하는 부분 강조(하이라이트) 표시
3. **미리보기 기능**: 결과 클릭 시 페이지 이동 없이 팝업(모달)으로 해당 조항/규정 전문 표시 (FR-05)
4. **관련 사이트로 이동**: 결과마다 대학 규정 페이지 또는 산학협력단 게시판 원본으로 가는 링크 버튼 (FR-04)
5. **반응형 디자인**: PC와 모바일 모두에서 사용 가능하게 구성 (NFR-04)

### 관련 요구사항
- FR-03 (결과 상세 표시), FR-04 (사이트 이동), FR-05 (미리보기), FR-07 (조회 형식 UI), NFR-03 (일관성), NFR-04 (접근성)

### 완료 기준
- ✅ 웹 브라우저에서 "연구비" 검색 시 PRD의 예시 화면과 같은 결과가 표시된다
- ✅ 미리보기 팝업과 원본 사이트 이동 링크가 정상 동작한다
- ✅ 휴대폰 화면에서도 정상적으로 사용할 수 있다

---

## Task 5. 검색 품질 검증 및 운영(데이터 갱신) 체계 마련

> **쉽게 말하면**: 시스템을 실제로 써보며 **"제대로 찾아주는지" 점검하고 다듬는 마무리 단계**입니다. 또한 규정은 계속 개정되므로, 규정이 바뀌었을 때 **관리자가 쉽게 최신 내용으로 갱신하는 방법**을 만들어 둡니다.

### 구현 내용
1. **검색 품질 평가**: 주요 키워드(연구비, 휴학, 장학, 등록금 등)로 테스트 검색 목록을 만들어, 관련 조항이 상위 5건 안에 나오는지 점검 (NFR-02)
2. **검색 튜닝**: 평가 결과를 바탕으로 청킹 방식, 의미 검색/키워드 검색의 비중, 결과 정렬 기준 등을 조정하여 정확도 개선
3. **데이터 갱신 절차 구축**: 규정 제·개정 시 관리자가 파이프라인(수집→청킹→임베딩)을 한 번에 재실행할 수 있는 갱신 스크립트/배치 마련 (DR-05)
4. **담당부서 정보 검수**: 검색 결과에 표시되는 담당부서·연락처가 실제와 일치하는지 총무과·산학연구과와 확인
5. **운영 문서화**: 시스템 사용법과 데이터 갱신 방법을 정리한 운영 매뉴얼 작성

### 관련 요구사항
- NFR-01 (응답 속도), NFR-02 (검색 정확도), DR-05 (데이터 갱신), 6.2 성공 지표 전반

### 완료 기준
- ✅ 주요 키워드 검색 시 관련 조항이 상위 5건 내에 노출된다 (성공 지표 달성)
- ✅ 관리자가 명령 한 번으로 규정 데이터를 최신으로 갱신할 수 있다
- ✅ 운영 매뉴얼이 작성되어 있다

---

## 단계 간 의존 관계

| 단계 | 앞 단계 결과물 활용 |
|---|---|
| Task 1 | (시작점) 원본 사이트 2곳에서 데이터 확보 |
| Task 2 | Task 1의 정리된 규정 텍스트 + 메타데이터를 청킹·임베딩 |
| Task 3 | Task 2의 벡터DB를 검색하는 API 개발 |
| Task 4 | Task 3의 API를 호출하는 웹 화면 개발 |
| Task 5 | Task 1~4 전체를 대상으로 품질 검증 및 갱신 체계 구축 |

> 📌 본 문서는 **작업 단위와 순서 정의**까지만 다루며, 실제 코드 작성은 각 Task 착수 시 진행한다. 기술 스택(임베딩 모델, 벡터DB 종류, 웹 프레임워크 등)은 Task 1~2 착수 전에 확정한다.
