# 운영 매뉴얼 (비전공자용) — AI 기반 대학·산학협력단 규정 통합 검색 시스템

이 문서는 프로그래밍을 잘 모르는 담당자도 시스템을 **실행**하고,
규정이 바뀌었을 때 **데이터를 갱신**하고, 문제가 생겼을 때 **해결**할 수 있도록 만든 안내서입니다.

---

## 1. 처음 한 번만 하면 되는 준비 작업

컴퓨터에 파이썬(Python) 프로그램과, 검색 기능에 필요한 부품(라이브러리)을 설치합니다.
**한 번만 설치하면 다음부터는 이 과정을 다시 할 필요가 없습니다.**

1. 이 프로젝트 폴더를 명령 프롬프트(또는 PowerShell)에서 엽니다.
2. 아래 명령을 순서대로 입력합니다. (인터넷 연결 필요, 처음 한 번은 몇 분 정도 걸릴 수 있습니다)

```powershell
pip install sentence-transformers chromadb fastapi uvicorn
```

---

## 2. 검색 시스템 실행하는 방법 (평상시)

컴퓨터를 새로 켰거나, 서버가 꺼져 있을 때 아래처럼 실행합니다.

1. 프로젝트 폴더에서 다음 명령을 입력합니다.

   ```powershell
   python -m uvicorn scripts.api_server:app --port 8000
   ```

2. 화면에 아래와 같은 문구가 나오면 정상적으로 켜진 것입니다. (30~60초 정도 걸릴 수 있습니다)

   ```
   Uvicorn running on http://127.0.0.1:8000
   ```

3. 인터넷 브라우저(크롬 등)를 열고 주소창에 아래 주소를 입력합니다.

   ```
   http://127.0.0.1:8000/
   ```

4. 검색창이 보이면 정상입니다. "연구비", "휴학" 등을 검색해보세요.

> ⚠️ 이 창을 닫으면(또는 컴퓨터를 끄면) 검색 서비스도 함께 꺼집니다.
> 계속 서비스하려면 이 프로그램이 항상 켜져 있는 컴퓨터(서버)에서 실행해야 합니다.

---

## 3. 규정이 바뀌었을 때 — 데이터 갱신 방법

대학이나 산학협력단의 규정이 새로 만들어지거나(제정), 내용이 바뀌면(개정),
검색 시스템의 내용도 최신으로 업데이트해야 합니다. **아래 명령 한 줄이면 전부 자동으로 처리됩니다.**

### 3-1. 전체 갱신 (가장 흔하게 쓰는 방법)

```powershell
python scripts/refresh_all.py
```

이 명령 하나가 자동으로 다음 순서를 전부 실행합니다.

```
① 대학·산학협력단 홈페이지에서 규정 새로 받아오기
② 받은 규정을 조항(제1조, 제2조...) 단위로 자르기
③ AI가 이해할 수 있는 형태로 변환해서 검색 데이터베이스에 저장하기
④ 검색이 잘 되는지 자동으로 점검하기
```

완료까지 대략 **20~40분** 정도 걸립니다 (규정 전체를 다시 받아오기 때문입니다).
중간에 인터넷이 잠깐 끊겨도 이미 받은 규정은 다시 받지 않고 이어서 진행합니다.

### 3-2. 특정 규정 하나만 빠르게 갱신하고 싶을 때

예를 들어 "인권센터 규정"만 방금 개정되었다면:

```powershell
python scripts/collect_all.py
python scripts/refresh_all.py --skip-collect --name 인권센터
```

- 첫 줄: 홈페이지에서 최신 규정을 다시 받아옵니다 (전체를 다시 받되, 이미 있는 것은 건너뜁니다)
- 둘째 줄: "인권센터"라는 이름이 들어간 규정만 조항 자르기 → 검색 데이터베이스 반영을 다시 합니다 (1분 이내)

### 3-3. 갱신이 잘 되었는지 확인하는 방법

`refresh_all.py`를 실행하면 마지막에 아래처럼 표가 나옵니다. 모두 "성공"이면 정상입니다.

```
[전체 갱신 요약]
  수집            : 성공     (250.0초)
  청킹            : 성공     (5.0초)
  임베딩·벡터DB      : 성공     (500.0초)
  품질평가          : 성공     (2.0초)
```

만약 "실패"라고 나오면 **8번 문제 해결 방법**을 참고하세요.

---

## 4. 검색이 잘 되고 있는지 점검하는 방법 (품질 검사)

시스템을 오래 운영하다 보면 "검색이 정확한가?"를 다시 확인하고 싶을 수 있습니다.
아래 명령으로 자동 점검할 수 있습니다. (서버를 켜 둔 상태에서 실행)

```powershell
python scripts/evaluate_search.py
```

- "연구비", "휴학", "장학" 등 자주 쓰는 검색어 15개를 자동으로 검색해보고,
  관련 있는 규정이 **상위 5건 안에 나오는지** 점검합니다.
- 결과 예시:

  ```
  [요약] 성공률: 15/15 (100.0%) — PRD 6.2 목표: 상위 5건 내 노출
  [요약] 평균 응답시간: 0.06초 — PRD 6.2 목표: 평균 3.0초 이내
  [판정] 성공률 100%: PASS / 응답속도 목표: PASS
  ```

- 결과는 `data/search_quality_report.json` 파일에도 저장됩니다.
- 만약 특정 검색어의 성공(✅) 표시가 실패(❌)로 나온다면, 개발 담당자에게
  `scripts/search_engine.py`의 검색 방식(의미 검색·키워드 검색 비중) 점검을 요청하세요.

---

## 5. 담당부서·연락처가 맞는지 확인하는 방법

검색 결과에는 각 규정의 담당부서와 전화번호가 함께 표시됩니다.
이 정보가 실제와 맞는지 총무과·산학연구과에 검수를 요청할 때 아래 파일을 사용하세요.

```powershell
python scripts/export_contacts.py
```

실행하면 다음 두 파일이 만들어집니다.

| 파일 | 용도 |
|---|---|
| `data/contacts_for_review.csv` | 엑셀로 열어서 검토·회신할 때 사용 |
| `data/contacts_for_review.md` | 이메일이나 공문에 표 형태로 붙여넣을 때 사용 |

표에는 **출처(대학/산학협력단) · 담당부서 · 현재 연락처 · 해당 규정 수 · 규정명 예시**가 정리되어 있고,
'검수결과(O/X)'와 '수정할 연락처' 칸이 비어 있어 담당자가 바로 채워서 회신할 수 있습니다.

> 📌 참고: 대학 규정은 원본 사이트(경상국립대 홈페이지)에 규정별 담당 전화번호가 따로
> 게시되어 있지 않아, 현재는 사이트 하단에 공통 게시된 **총무과(055-772-0334)** 번호로
> 표시됩니다. 규정별 정확한 연락처가 확인되면 검수 결과를 반영해 데이터를 갱신할 수 있습니다.

---

## 6. 자주 발생하는 문제와 해결 방법

### Q1. 브라우저에서 `http://127.0.0.1:8000/`이 안 열려요.
- 서버(2번 항목)를 먼저 실행했는지 확인하세요. 검은 화면(명령 프롬프트)에
  `Uvicorn running on http://127.0.0.1:8000` 문구가 보여야 합니다.
- 서버를 처음 켤 때는 30~60초 정도 기다려야 합니다(AI 모델을 불러오는 시간).

### Q2. 검색해도 결과가 하나도 안 나와요.
- 정말 관련 없는 검색어라면 정상입니다 (예: "오늘 날씨" 검색 시 결과 없음이 맞습니다).
- 평소 잘 나오던 검색어도 안 나온다면, 서버를 껐다가(창 닫기) 다시 켜보세요.

### Q3. 화면에 한글이 깨져서(물음표나 이상한 기호) 보여요.
- 브라우저 화면은 정상 UTF-8로 표시되므로 문제가 없어야 합니다.
- 만약 **명령 프롬프트 화면**(검은 창)의 글자가 깨진다면, 명령 프롬프트 설정 문제이며
  실제 검색 기능에는 영향이 없습니다. `chcp 65001` 명령을 먼저 입력한 뒤 실행하면 완화됩니다.

### Q4. `python` 명령이 없다고 나와요 ("python은 내부 또는 외부 명령이 아닙니다").
- 컴퓨터에 파이썬(Python)이 설치되어 있지 않은 것입니다. 정보전산처에 파이썬 설치를 요청하세요.

### Q5. `pip install`이 오류가 나거나 너무 오래 걸려요.
- 인터넷 연결 상태를 확인하세요. 사내망에서 외부 접속이 막혀 있으면
  정보전산처에 방화벽/프록시 설정을 문의하세요.

### Q6. `refresh_all.py` 실행 중 "실패"가 나왔어요.
- 어느 단계(수집/청킹/임베딩·벡터DB/품질평가)에서 실패했는지 확인하세요.
- **수집 실패**: 대학·산학협력단 홈페이지 구조가 바뀌었을 수 있습니다.
  `data/errors_university.json`, `data/errors_foundation.json` 파일에서 실패한 규정과
  원인을 확인해 개발 담당자에게 전달하세요.
- **청킹/임베딩 실패**: 보통 일시적 오류이므로 같은 명령을 한 번 더 실행해보세요.

### Q7. 검색 결과의 담당부서 연락처가 실제와 달라요.
- 5번 항목의 검수용 파일을 이용해 정정 요청을 받은 뒤, 개발 담당자에게
  `data/regulations.json`의 해당 항목 수정 및 재적재(`refresh_all.py --skip-collect`)를 요청하세요.

### Q8. 서버를 껐다가 다시 켰는데 이전 검색 결과가 그대로예요.
- 정상입니다. 검색 데이터(`data/`, `C:\gnu_vectordb`)는 파일로 저장되어 있어
  서버를 껐다 켜도 유지됩니다. 규정 내용을 바꾸려면 3번(데이터 갱신) 항목을 참고하세요.

---

## 7. 시스템 구성 요약 (참고용)

| 항목 | 위치 |
|---|---|
| 규정 원문 텍스트 | `data/university/`, `data/foundation/` |
| 전체 규정 목록 | `data/regulations.json` |
| 조항 단위로 잘린 데이터 | `data/chunks.json` |
| 검색용 AI 데이터베이스(벡터DB) | `C:\gnu_vectordb` |
| 검색 서버 프로그램 | `scripts/api_server.py` |
| 웹 화면 | `web/index.html`, `web/style.css`, `web/app.js` |
| 검색 품질 점검 결과 | `data/search_quality_report.json` |
| 담당부서 검수용 파일 | `data/contacts_for_review.csv`, `.md` |

문의: 정보전산처 (시스템 운영) / 총무과 055-772-0334 (대학 규정) / 산학연구과 055-772-0211 (산학협력단 규정)
