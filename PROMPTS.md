# PROMPTS: 단계별 구현 요청 프롬프트 모음 (T1 ~ T10)

- **기준 문서**: `PRD.md` (v1.0), `TASKS.md`
- **작성일**: 2026-08-31
- **최종 갱신**: T1~T5.5(검색 MVP + 디자인 개편), T6~T10(RAG 확장) 모두 구현 완료

## 사용 방법 (꼭 읽어주세요)

1. 아래 프롬프트를 **T1부터 순서대로 하나씩** 복사하여 AI(코딩 어시스턴트)에게 붙여넣으세요.
2. 한 단계가 끝나면 각 프롬프트 아래의 **[확인 방법]** 대로 결과를 직접 확인하세요.
3. 결과가 정상이면 다음 단계 프롬프트로 진행하고, 문제가 있으면 **같은 단계에서 수정을 요청**한 뒤 넘어가세요.
4. 절대로 두 단계를 한 번에 요청하지 마세요. (앞 단계 결과물이 다음 단계의 재료가 됩니다)
5. **매 단계(Tn) 개발이 끝나면 `README.md`의 "진행 현황" 표와 "Tn 검증 결과 요약" 섹션을 함께 갱신**하세요. (문서가 실제 구현 상태와 어긋나지 않도록 유지)

```
[MVP 완료]
T1 데이터 수집 → 확인 → T2 청킹·벡터DB → 확인 → T3 검색 API → 확인 → T4 웹 화면 → 확인 → T5 품질검증·운영 → T5.5 디자인 개편

[RAG 확장 — 계획 단계, 아래 T6~T10]
T6 RAG 파이프라인 기반 구축 → 확인 → T7 답변 생성 API → 확인 → T8 챗봇 웹 화면 → 확인 → T9 품질검증·환각방지 → 확인 → T10 운영체계 마련
```

> ✅ **T6~T10 모두 구현 완료되었습니다.** 아래 프롬프트는 각 단계를 착수했던 시점에 실행한 기록입니다.

---

## T1. 규정 데이터 수집 및 텍스트 정리 요청 프롬프트

> 💡 이 단계가 끝나면: 두 사이트의 규정이 컴퓨터가 읽을 수 있는 텍스트 파일로 정리되어 폴더에 저장됩니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md와 TASKS.md를 먼저 읽고, TASKS.md의 Task 1(규정 데이터 수집 및 텍스트 정리)을 구현해줘.

[해야 할 일]
1. 아래 두 사이트에서 규정 데이터를 수집하는 프로그램을 만들어줘.
   - 대학 규정: https://www.gnu.ac.kr/main/cm/cntnts/cntntsView.do?mi=1255&cntntsId=1214
     (학칙/규정/지침 탭으로 구분된 페이지에서 규정 목록과 원문 파일을 수집)
   - 산학협력단 규정: https://www.gnu.ac.kr/research/na/ntt/selectNttList.do?mi=7117&bbsId=2456
     (게시판에서 가장 최신 기준일자의 '산학협력단 규정집' 게시글 첨부파일을 다운로드)
2. 다운로드한 HWP/PDF 파일에서 텍스트를 추출하고, 깨진 글자나 불필요한 서식을 정리해줘.
3. 규정마다 다음 정보(메타데이터)를 함께 정리해줘:
   출처(대학/산학협력단), 카테고리(학칙/규정/지침), 규정명, 담당부서와 연락처, 원본 페이지 URL
4. 결과는 data 폴더 아래에 규정별 텍스트 파일과 전체 목록 JSON 파일(regulations.json)로 저장해줘.

[조건]
- 두 사이트는 구조가 다르므로 수집 프로그램을 각각 나눠서 만들어줘.
- 사이트 접속이 안 되거나 파일 형식이 예상과 다른 경우를 대비해 오류 처리를 넣어줘.
- 나중에 규정이 개정되면 다시 실행할 수 있도록, 명령 한 번으로 전체 수집이 다시 되도록 만들어줘.
- 실행 방법을 README 또는 주석으로 쉽게 설명해줘.
- 완료 후 수집된 규정 개수와 저장 위치를 알려줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [ ] `data` 폴더에 규정 텍스트 파일들과 `regulations.json`이 생성되었는가?
- [ ] `regulations.json`을 열어보면 규정마다 출처·카테고리·규정명·담당부서·URL이 들어 있는가?
- [ ] 텍스트 파일 몇 개를 열어 내용이 깨지지 않고 읽히는가?
- [ ] 대학 규정과 산학협력단 규정이 모두 수집되었는가? (개수 확인)

---

## T2. 조·항 단위 청킹 및 벡터DB 구축 요청 프롬프트

> 💡 이 단계가 끝나면: 규정이 '제3조 제2항' 같은 조항 단위로 잘게 나뉘어, AI가 의미로 검색할 수 있는 데이터베이스(벡터DB)에 저장됩니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md와 TASKS.md를 먼저 읽고, TASKS.md의 Task 2(조·항 단위 청킹 및 벡터DB 구축)를 구현해줘.
T1에서 만든 data 폴더의 규정 텍스트와 regulations.json을 재료로 사용해.

[해야 할 일]
1. 규정 텍스트를 '제0조(제목)', '제0항' 같은 조문 패턴을 인식해서 조·항 단위로 잘라주는(청킹) 프로그램을 만들어줘.
   - 부칙, 별표처럼 조문 구조가 다른 부분도 빠뜨리지 않게 처리해줘.
2. 잘라낸 조각(청크)마다 다음 정보를 붙여줘:
   출처(대학/산학협력단), 규정 구분(카테고리), 규정명, 조·항 번호, 담당부서와 연락처, 원본 페이지 URL
3. 각 청크를 한국어를 잘 이해하는 임베딩 모델로 벡터(의미를 담은 숫자)로 바꾸고, 벡터DB에 저장해줘.
   - 임베딩 모델과 벡터DB는 무료로 로컬에서 실행 가능한 것 중에서 추천해서 사용하고, 왜 그것을 골랐는지 설명해줘.
4. 저장이 끝나면 "연구비"라는 단어로 벡터DB를 테스트 검색해서, 관련 조항이 나오는지 결과를 보여줘.

[조건]
- 규정이 개정되면 특정 규정만 다시 잘라서 다시 저장할 수 있는 구조로 만들어줘.
- 청킹 결과(전체 청크 수, 규정별 청크 수)를 요약해서 보여줘.
- 조문 패턴 인식에 실패한 규정이 있으면 목록으로 알려줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [ ] 전체 청크 수와 규정별 청크 수 요약이 출력되었는가?
- [ ] 테스트 검색("연구비")에서 실제로 관련 있는 조항이 나오는가?
- [ ] 청크에 규정명·조항 번호·담당부서 등 정보가 모두 붙어 있는가?
- [ ] 청킹에 실패한 규정 목록이 있다면, 수정을 요청하여 해결했는가?

---

## T3. 통합 검색 엔진(백엔드 API) 개발 요청 프롬프트

> 💡 이 단계가 끝나면: 검색어를 보내면 관련 조항을 찾아서 돌려주는 '검색 서버(두뇌)'가 완성됩니다. 아직 화면은 없습니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md와 TASKS.md를 먼저 읽고, TASKS.md의 Task 3(통합 검색 엔진 API 개발)을 구현해줘.
T2에서 만든 벡터DB를 사용해서 검색하는 웹 API 서버를 만들어줘.

[해야 할 일]
1. 검색어를 받으면 관련 조항을 찾아주는 검색 API를 만들어줘.
   - "연구비" 같은 단어 검색과 "휴학하려면 어떻게 해?" 같은 문장 검색이 모두 잘 되도록,
     의미 기반 검색(벡터 유사도)과 키워드 검색을 결합한 하이브리드 검색으로 만들어줘.
2. 검색 결과에는 각 조항마다 다음 정보가 포함되어야 해 (PRD의 FR-03 참고):
   규정 구분, 규정명(관련 규정 모두 표시), 조항 번호(제0조 제0항), 본문 발췌, 담당부서와 연락처, 원본 페이지 URL
3. 카테고리(학칙/규정/지침)와 출처(대학/산학협력단)로 결과를 좁힐 수 있는 필터 기능을 넣어줘.
4. 특정 조항을 선택하면 그 조항(또는 규정 전문)을 돌려주는 미리보기용 상세 조회 API도 만들어줘.
5. 서버 실행 방법과 API 사용 예시(주소, 보내는 값, 받는 값)를 문서로 정리해줘.

[조건]
- 검색 결과는 3초 이내에 반환되어야 해 (PRD의 NFR-01).
- 완성되면 "연구비"로 테스트 검색을 실행해서, PRD 4.1의 검색 결과 표시 예시처럼
  [규정 구분 / 규정명 / 조항·본문 / 담당부서] 형태로 결과가 나오는지 보여줘.
- 검색 결과가 없을 때의 응답도 처리해줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [ ] 서버를 실행하고 "연구비"로 검색하면 PRD 예시와 같은 형태의 결과가 나오는가?
- [ ] 문장형 질문(예: "휴학하려면 어떻게 해?")으로도 관련 조항이 나오는가?
- [ ] 카테고리/출처 필터가 동작하는가?
- [ ] 미리보기(상세 조회) API가 조항 전문을 돌려주는가?
- [ ] 검색 응답이 3초 안에 오는가?

---

## T4. 통합 검색 웹 화면(프론트엔드 UI) 개발 요청 프롬프트

> 💡 이 단계가 끝나면: 학생·교직원 누구나 브라우저에서 쓸 수 있는 실제 검색 페이지가 완성됩니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md와 TASKS.md를 먼저 읽고, TASKS.md의 Task 4(통합 검색 웹 화면 개발)를 구현해줘.
T3에서 만든 검색 API를 호출하는 웹 화면을 만들어줘.
조회 형식은 연세대학교 규정집 사이트(https://rules.yonsei.ac.kr/)를 참고해서 깔끔하게 구성해줘.

[해야 할 일]
1. 검색 메인 화면: 가운데에 큰 통합 검색창 하나와,
   카테고리(학칙/규정/지침)·출처(대학/산학협력단) 필터를 배치해줘.
2. 검색 결과 목록: 결과마다 카드 형태로 다음을 표시해줘.
   - 규정 구분(배지 형태) / 규정명 / '제0조 제0항' 본문 발췌 / 담당부서(연락처)
   - 검색어와 일치하는 부분은 형광펜처럼 강조(하이라이트)해줘.
3. 미리보기 기능: 결과를 클릭하면 페이지 이동 없이 팝업(모달)으로 해당 조항과 규정 전문을 보여줘.
4. 관련 사이트로 이동: 결과마다 원본 규정 페이지(대학 규정 페이지 또는 산학협력단 게시판)로
   새 창으로 이동하는 버튼을 넣어줘.
5. PC와 휴대폰 화면 모두에서 보기 좋게(반응형) 만들어줘.

[조건]
- 검색 결과가 없을 때는 "검색 결과가 없습니다" 안내와 함께 다른 검색어를 제안해줘.
- 검색 중에는 로딩 표시를 보여줘.
- 완성되면 실행 방법을 알려주고, 브라우저에서 "연구비"를 검색한 화면이 PRD 4.1의 예시처럼 나오는지 확인해줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [ ] 브라우저에서 검색 페이지가 열리고 "연구비" 검색 시 결과 카드가 표시되는가?
- [ ] 결과에 규정 구분·규정명·조항·본문·담당부서(연락처)가 모두 보이는가?
- [ ] 검색어가 형광펜처럼 강조되어 표시되는가?
- [ ] 미리보기 팝업과 '관련 사이트로 이동' 버튼이 정상 동작하는가?
- [ ] 휴대폰 크기 화면(브라우저 창을 좁혀서)에서도 잘 보이는가?

---

## T5. 검색 품질 검증 및 운영 체계 마련 요청 프롬프트

> 💡 이 단계가 끝나면: 검색이 정확한지 점검·개선되고, 규정이 개정됐을 때 관리자가 쉽게 최신화할 수 있는 절차와 매뉴얼이 완성됩니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md와 TASKS.md를 먼저 읽고, TASKS.md의 Task 5(검색 품질 검증 및 운영 체계 마련)를 구현해줘.
T1~T4에서 만든 전체 시스템을 대상으로 진행해줘.

[해야 할 일]
1. 검색 품질 평가: 주요 키워드(연구비, 휴학, 장학, 등록금, 성적, 수강신청, 연구윤리 등)로
   테스트 검색 목록을 만들고, 각 키워드마다 관련 조항이 상위 5건 안에 나오는지 자동으로
   점검하는 평가 스크립트를 만들어서 실행해줘. 결과를 표로 정리해줘.
2. 검색 튜닝: 평가 결과가 나쁜 키워드가 있으면 원인을 분석하고
   (청킹 방식, 의미 검색/키워드 검색 비중, 결과 정렬 기준 등을 조정해서) 정확도를 개선해줘.
   개선 전/후 결과를 비교해서 보여줘.
3. 데이터 갱신 절차: 규정이 개정되었을 때 관리자가 명령 한 번으로
   [수집 → 청킹 → 임베딩 → 벡터DB 갱신]을 다시 실행할 수 있는 갱신 스크립트를 만들어줘.
4. 검색 결과에 표시되는 담당부서·연락처 목록을 뽑아줘.
   (총무과·산학연구과에 실제와 맞는지 검수를 요청할 수 있도록 표 형태로)
5. 운영 매뉴얼 작성: 비전공자 관리자도 따라할 수 있도록
   시스템 실행 방법, 데이터 갱신 방법, 자주 발생하는 문제 해결법을 담은 운영 매뉴얼(MANUAL.md)을 만들어줘.

[조건]
- 평가 기준은 PRD 6.2의 성공 지표를 따라줘 (상위 5건 내 노출, 평균 응답 3초 이내).
- 응답 속도도 함께 측정해서 결과에 포함해줘.
```

### ✅ 확인 방법 (프로젝트 마무리 점검)
- [ ] 주요 키워드 평가 결과표에서 관련 조항이 상위 5건 내에 노출되는가?
- [ ] 평균 검색 응답 시간이 3초 이내인가?
- [ ] 갱신 스크립트를 실행하면 데이터가 처음부터 끝까지 다시 만들어지는가?
- [ ] 담당부서·연락처 검수용 표가 만들어졌는가? (→ 총무과·산학연구과에 검수 의뢰)
- [ ] MANUAL.md를 읽고 비전공자도 시스템을 실행·갱신할 수 있겠는가?

---

## 🔽 여기부터 T6 ~ T10: RAG(생성형 답변) 확장 (모두 구현 완료)

> PRD.md 5.2(제외 범위)에 명시된 "챗봇형 Q&A(RAG 기반 답변 생성)" 후속 단계에 해당합니다.
> T1~T5.5에서 만든 **검색(Search) 기능은 그대로 유지**하고, 그 위에 **답변 생성(Generation) 기능을 추가**하는 방식입니다.
> (RAG = Retrieval-Augmented Generation: T2에서 만든 벡터DB로 관련 조항을 "검색"하고, 그 조항만 근거로 삼아 LLM이 "답변을 생성"하는 방식. 검색 없이 LLM 혼자 답하게 하면 규정에 없는 내용을 지어내는 "환각(hallucination)" 위험이 크므로, 반드시 검색 결과를 근거로만 답하도록 강제하는 것이 이번 확장의 핵심 설계 원칙입니다.)

### RAG 확장 전체 흐름

```
[T6] RAG 파이프라인 기반 구축     →  검색(기존 T3) + LLM 답변 생성을 연결하는 핵심 엔진 개발
[T7] 답변 생성 API 개발          →  /api/ask 엔드포인트로 "질문 → 근거조항 → AI 답변"을 반환
[T8] RAG 챗봇 웹 화면 개발        →  기존 검색 화면(T4/T5.5)에 대화형 질문·답변 UI 추가
[T9] 품질 검증 및 환각 방지 튜닝   →  답변이 실제 조항에 근거하는지 자동 검증, 안 되면 "모른다"고 답하게 튜닝
[T10] 운영 체계 마련              →  로그·비용 관리, 장애 시 검색 전용 모드로 자동 전환, 매뉴얼 갱신
```

### 사전 결정 사항 (T6 착수 전 확정)

| 항목 | 결정 | 근거 |
|---|---|---|
| LLM 방식 | **플러그블(교체 가능) 구조**로, ① 로컬 오픈소스 LLM(무료, 오프라인, 느림)과 ② 외부 API(OpenAI 등 호환, 유료, 빠름·고품질) **둘 다 지원**하되 기본값은 로컬 | 현재 환경에 `OPENAI_API_KEY` 등 유료 API 키가 설정되어 있지 않고(`torch`/`transformers`는 이미 설치되어 있음), 학내 규정 데이터를 외부로 보내지 않아야 하는 보안 요구도 고려. 예산이 확보되면 환경변수 하나로 API 방식 전환 가능하게 설계 |
| 검색 로직 | T2/T3에서 만든 벡터DB·하이브리드 검색(`search_engine.py`)을 **그대로 재사용** | 이미 검증된(T5 품질평가 100%) 검색 로직을 다시 만들지 않고, 그 위에 답변 생성만 얹음 (중복 개발 방지, NFR-05 유지보수성) |
| 환각 방지 원칙 | ① 검색된 조항 밖의 내용은 절대 지어내지 말 것 ② 관련 조항이 없으면 "모른다"고 답할 것 ③ 답변에는 반드시 근거 조항(규정명·조항번호)을 출처로 표시할 것 | PRD가 우려하는 "규정에 없는 답변" 리스크 제거. 검색(T1~T5.5)의 정확성 신뢰도를 생성 답변에도 그대로 이어감 |
| 기존 기능 영향 | 기존 `/api/search` 및 검색 화면은 **변경 없이 그대로 유지**, `/api/ask`는 **추가(신규)** 엔드포인트 | 검색만으로 충분한 사용자도 있으므로 RAG는 선택적 부가 기능으로 설계 (점진적 확장) |

---

## T6. RAG 파이프라인 기반 구축 (검색 + LLM 연결 엔진)

> 💡 이 단계가 끝나면: 질문을 넣으면 ① 관련 조항을 검색하고 ② 그 조항만 근거로 삼아 AI가 답변 문장을 만들어내는 **핵심 엔진(`rag_engine.py`)**이 완성됩니다. 아직 웹 API·화면은 없고, 터미널에서 테스트만 가능한 단계입니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md, TASKS.md, README.md(T1~T5.5 진행 현황)를 먼저 읽고,
PROMPTS.md의 T6(RAG 파이프라인 기반 구축)을 구현해줘.
T2~T3에서 만든 벡터DB와 scripts/search_engine.py의 검색 로직을 그대로 재사용해줘.

[해야 할 일]
1. scripts/rag_engine.py 파일을 새로 만들어줘. 다음 3가지 역할을 순서대로 수행해야 해.
   ① 검색(Retrieval): 사용자 질문을 scripts/search_engine.py의 하이브리드 검색에 넣어 관련 조항 상위 N개를 가져온다.
   ② 컨텍스트 조립(Context Building): 가져온 조항들을 "[규정명] 제0조(제목): 본문..." 형식으로 정리해 LLM에게 줄 참고자료 텍스트를 만든다.
   ③ 답변 생성(Generation): "너는 대학 규정 안내원이다. 아래 [참고자료]에 있는 내용만 근거로 답하라.
      참고자료에 없는 내용은 답하지 말고 '해당 내용은 규정에서 찾을 수 없습니다'라고 답하라.
      답변 끝에는 근거로 사용한 규정명과 조항을 반드시 표시하라." 같은 지침을 담은 프롬프트 템플릿을 만들고,
      LLM에게 보내 답변을 생성한다.
2. LLM 연결부는 나중에 로컬 모델/외부 API를 자유롭게 바꿔 낄 수 있도록 추상화(교체 가능한 구조)해줘.
   - 기본값: 로컬에서 무료로 실행 가능한 한국어 지원 오픈소스 모델(허깅페이스 transformers 활용)
   - 옵션: 환경변수(API 키)가 설정되어 있으면 외부 API(OpenAI 호환 방식)를 대신 쓸 수 있게
   - 어느 쪽도 사용할 수 없는 환경이면(모델 다운로드 실패 등) 에러 대신 "생성 불가, 검색 결과만 반환" 식으로 안전하게 처리해줘.
3. 관련 조항이 검색되지 않거나 관련성이 낮으면(기존 검색의 관련성 기준 재사용) LLM을 호출하지 말고
   바로 "규정에서 관련 내용을 찾을 수 없습니다"라고 답하도록 만들어줘. (불필요한 LLM 호출 방지 + 환각 예방)
4. rag_engine.py를 스크립트로 직접 실행하면 "연구비 지원 한도가 얼마야?" 같은 질문을 테스트로 넣어
   ① 검색된 조항 목록 ② 조립된 컨텍스트 ③ 최종 답변(+근거 출처)을 순서대로 출력하도록 만들어줘.

[조건]
- 기존 scripts/search_engine.py, scripts/api_server.py, web/ 폴더는 건드리지 마 (T7에서 연결할 예정).
- 사용한 LLM(모델명 또는 API)과 선택 이유를 설명해줘.
- 완료 후 이 단계의 구현 내용을 README.md의 "진행 현황" 표와 "T6 검증 결과 요약" 섹션에 반영해줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [x] `python scripts/rag_engine.py` 실행 시 검색된 조항 → 조립된 컨텍스트 → 최종 답변이 순서대로 출력되는가?
- [x] 답변 끝에 근거 규정명·조항번호가 표시되는가?
- [x] 참고자료에 없는 내용을 지어내지 않고, 관련 조항이 없을 때 "찾을 수 없습니다"라고 답하는가?
- [x] README.md가 T6 완료 상태로 갱신되었는가?

---

## T7. RAG 답변 생성 API 개발 (`/api/ask`)

> 💡 이 단계가 끝나면: T6의 엔진을 웹에서 호출할 수 있는 API(`/api/ask`)가 완성됩니다. 기존 검색 API(`/api/search`)는 그대로 유지된 채, "질문하면 AI가 답해주는" 새 창구가 하나 더 생기는 단계입니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md, README.md(T6 결과)를 먼저 읽고,
PROMPTS.md의 T7(RAG 답변 생성 API 개발)을 구현해줘.
T6에서 만든 scripts/rag_engine.py를 scripts/api_server.py에 연결해줘.

[해야 할 일]
1. 기존 scripts/api_server.py에 새 엔드포인트 POST(또는 GET) /api/ask 를 추가해줘.
   - 입력: 질문(question), 선택적으로 출처/카테고리 필터(기존 /api/search와 동일한 필터 방식 재사용)
   - 출력: AI 답변 문장, 근거로 사용한 조항 목록(규정 구분·규정명·조항번호·본문 발췌·담당부서·원본 URL — 기존 검색 결과 항목과 동일한 형식), 응답 소요 시간
2. 관련 조항이 없거나 관련성이 낮으면 답변 대신 "찾을 수 없음" 응답과 함께, 대신 참고할 만한 추천 검색어를 안내해줘.
3. 기존 /api/search, /api/chunks, /api/regulations 등 다른 API는 절대 변경하지 마.
4. API 사용 예시(요청/응답 형식)를 scripts/README.md에 추가해줘.
5. 응답 시간을 측정해서, LLM 답변 생성에 시간이 오래 걸릴 경우를 대비한 타임아웃/처리 방안도 넣어줘.

[조건]
- "연구비 지원 한도가 얼마야?", "휴학 신청은 언제까지 해야 해?" 같은 질문으로 테스트해서
  답변과 근거 조항이 실제로 관련 있는지 보여줘.
- "오늘 날씨 어때?"처럼 규정과 무관한 질문에는 답변을 지어내지 않고 "찾을 수 없음"으로 응답하는지 보여줘.
- 완료 후 README.md의 "진행 현황" 표와 "T7 검증 결과 요약" 섹션을 갱신해줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [x] `/api/ask`에 규정 관련 질문을 보내면 답변 + 근거 조항 목록이 함께 반환되는가?
- [x] 근거 조항의 형식(규정 구분·규정명·조항·담당부서 등)이 기존 검색 결과와 일관되는가?
- [x] 규정과 무관한 질문에는 지어내지 않고 "찾을 수 없음"으로 응답하는가?
- [x] 기존 `/api/search` 등 기존 기능이 그대로 동작하는가? (회귀 확인)
- [x] README.md가 T7 완료 상태로 갱신되었는가?

---

## T8. RAG 챗봇 웹 화면(UI) 개발

> 💡 이 단계가 끝나면: 기존 검색 화면(T4/T5.5)에서 검색창 옆(또는 탭 전환)으로 "질문하기" 모드를 선택해, 채팅하듯 질문하고 AI 답변과 출처 카드를 함께 볼 수 있는 화면이 완성됩니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md, README.md(T7 결과)를 먼저 읽고,
PROMPTS.md의 T8(RAG 챗봇 웹 화면 개발)을 구현해줘.
T7에서 만든 /api/ask API를 호출하는 화면을 web/ 폴더의 기존 검색 화면에 자연스럽게 추가해줘.

[해야 할 일]
1. 기존 검색창 영역에 "검색" / "AI에게 질문하기" 두 가지 모드를 탭으로 전환할 수 있게 해줘.
   (기존 검색 기능 화면·동작은 그대로 유지)
2. "AI에게 질문하기" 모드: 질문을 입력하면 답변이 채팅 메시지 형태로 표시되고,
   답변 아래에는 근거로 사용한 조항들을 기존 검색 결과 카드와 같은 디자인으로 나란히 보여줘.
3. AI가 생성한 답변임을 명확히 표시하는 안내 문구(예: "AI가 규정을 바탕으로 생성한 답변이며,
   법적 효력은 원본 규정을 따릅니다")를 답변마다 붙여줘.
4. 답변 생성 중에는 로딩(작성 중) 표시를 보여주고, 실패하거나 "찾을 수 없음"인 경우
   안내 메시지와 함께 일반 검색으로 전환할 수 있는 버튼을 보여줘.
5. 기존 GNU 브랜드 색상(T5.5의 style.css 변수)을 그대로 사용해서 디자인 일관성을 유지해줘.
6. PC와 모바일 모두에서 보기 좋게 반응형으로 만들어줘.

[조건]
- 기존 검색 모드 화면과 기능은 절대 깨뜨리지 마.
- 완성되면 실제 브라우저에서 "연구비 지원 한도가 얼마야?" 같은 질문을 해보고 스크린샷으로 확인해줘.
- 완료 후 README.md의 "진행 현황" 표와 "T8 검증 결과 요약" 섹션을 갱신해줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [x] "검색" ↔ "AI에게 질문하기" 모드 전환이 정상 동작하는가?
- [x] 질문 시 답변과 출처 카드가 함께 표시되는가?
- [x] "AI 생성 답변" 안내 문구가 매번 표시되는가?
- [x] 로딩 표시, 실패 시 안내가 정상 동작하는가?
- [x] 기존 검색 기능이 그대로 동작하는가? (회귀 확인)
- [x] 모바일 화면에서도 정상적으로 보이는가?
- [x] README.md가 T8 완료 상태로 갱신되었는가?

---

## T9. RAG 품질 검증 및 환각(hallucination) 방지 튜닝

> 💡 이 단계가 끝나면: AI 답변이 실제 규정 조항에 근거하고 있는지 자동으로 점검하고, 문제가 있으면 프롬프트·검색 기준을 다듬어 "규정에 없는 내용을 지어내는 사고"를 최소화하는 단계입니다. T5의 검색 품질 검증과 같은 원리를 답변 생성에도 적용합니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md, README.md(T8 결과)를 먼저 읽고,
PROMPTS.md의 T9(RAG 품질 검증 및 환각 방지 튜닝)을 구현해줘.
T6~T8에서 만든 RAG 파이프라인 전체를 대상으로 진행해줘.

[해야 할 일]
1. scripts/evaluate_search.py를 참고해서, RAG 답변 품질을 자동으로 점검하는
   scripts/evaluate_rag.py를 만들어줘. 다음을 점검해줘.
   - 정답 있는 질문 세트(예: "연구비 지원 한도는?", "휴학은 몇 학기까지 가능해?" 등 10~15개)를 만들어,
     답변이 실제로 관련 조항을 인용하는지(근거 조항 id가 정답 후보 조항과 일치하는지) 자동 채점
   - 답변 문장이 근거 조항 본문에 없는 사실을 포함하는지(환각) 간단히 점검
     (예: 답변에 등장하는 숫자·규정명이 근거 조항 본문에도 실제로 등장하는지 대조)
   - 규정과 무관한 질문(예: "오늘 날씨 어때?")에 대해 답변을 지어내지 않고 거절하는지 점검
   - 답변 생성까지 걸리는 시간 측정
2. 점검 결과를 표로 정리해서 보여주고, data/rag_quality_report.json으로 저장해줘.
3. 결과가 나쁘면(근거 없는 답변, 잘못된 거절/답변) 원인을 분석해서
   프롬프트 지침, 검색 결과 개수(top_k), 관련성 임계값 등을 조정해 개선해줘. 개선 전/후 결과를 비교해줘.
4. LLM이 근거 조항에 없는 규정명을 인용하면 자동으로 걸러내거나 경고를 붙이는
   안전장치(가드레일) 로직을 rag_engine.py에 추가해줘.

[조건]
- 기존 T5 검색 품질(100%)이 떨어지지 않는지 함께 재확인해줘 (evaluate_search.py 재실행).
- 완료 후 README.md의 "진행 현황" 표와 "T9 검증 결과 요약" 섹션을 갱신해줘.
```

### ✅ 확인 방법 (다음 단계로 가기 전에)
- [x] `python scripts/evaluate_rag.py` 실행 시 결과 표가 출력되는가?
- [x] 근거 조항과 무관한 내용(환각)이 크게 줄었는가? (무관한 질문 2건 모두 실패 → 튜닝 후 7/7 통과)
- [x] 무관한 질문에는 여전히 잘 거절하는가?
- [x] 기존 검색 품질(T5)이 그대로 유지되는가? (15/15, 100% 유지)
- [x] README.md가 T9 완료 상태로 갱신되었는가?

---

## T10. RAG 운영 체계 마련 (로그·비용·장애 대응·매뉴얼 갱신)

> 💡 이 단계가 끝나면: RAG 기능을 실제로 서비스에 올려도 관리자가 안심하고 운영할 수 있도록, 질문·답변 기록, LLM 장애 시 대응, 매뉴얼까지 정리하는 마무리 단계입니다. T5의 "운영 체계 마련"을 RAG 기능까지 확장하는 것입니다.

### 📋 복사해서 사용할 프롬프트

```
현재 프로젝트 폴더의 PRD.md, MANUAL.md, README.md(T9 결과)를 먼저 읽고,
PROMPTS.md의 T10(RAG 운영 체계 마련)을 구현해줘.
T6~T9에서 만든 RAG 기능 전체를 대상으로 진행해줘.

[해야 할 일]
1. 질문·답변 로그 기록: /api/ask 호출마다 [질문, 사용된 근거 조항 id들, 답변, 응답시간, 성공/거절 여부]를
   파일(예: data/rag_logs/)에 기록하는 기능을 추가해줘. (개인정보가 남지 않도록 질문자 식별정보는 저장하지 않음)
2. 외부 API를 쓰는 경우를 대비해, 호출 횟수·예상 비용을 집계해서 볼 수 있는 간단한 요약 스크립트를 만들어줘.
   (로컬 모델만 쓰는 경우엔 이 항목은 생략 가능하다고 안내해줘)
3. 장애 대응: LLM 호출이 실패하거나 너무 느릴 때 자동으로 "AI 답변 대신 검색 결과만 보여주는 모드"로
   전환되는 안전장치가 T7~T8에 이미 있는지 점검하고, 없으면 추가해줘.
4. MANUAL.md에 RAG 기능 관련 섹션을 추가해줘: 실행 방법(로컬 LLM 준비/외부 API 키 설정 방법),
   답변이 이상할 때 확인·보고하는 방법, 로그 확인 방법, 비용(외부 API 사용 시) 확인 방법.
5. 최종적으로 README.md의 "진행 현황" 표를 T10까지 전부 "완료"로 갱신하고,
   T6~T10 전체를 요약하는 "RAG 확장 검증 결과 요약" 섹션을 추가해줘.

[조건]
- 이 단계가 끝나면 T1~T10 전체가 "완료" 상태가 되어야 해.
- 운영 매뉴얼(MANUAL.md)만 보고도 비전공자 담당자가 RAG 기능을 켜고 끄고 점검할 수 있어야 해.
```

### ✅ 확인 방법 (프로젝트 마무리 점검)
- [x] 질문·답변 로그가 정상적으로 기록되는가? (개인정보 없이)
- [x] LLM 장애 시 검색 전용 모드로 자동 전환되는가?
- [x] MANUAL.md에 RAG 실행·문제해결 안내가 추가되었는가?
- [x] README.md의 진행 현황 표가 T10까지 모두 "완료"로 갱신되었는가?
- [x] T1~T10 전체 기능(검색 + RAG)이 함께 정상 동작하는가? (최종 회귀 확인)

---

## 부록: 진행 상황 체크리스트

| 단계 | 작업 | 완료 확인 | 완료일 |
|---|---|---|---|
| T1 | 규정 데이터 수집 및 텍스트 정리 | ☐ | |
| T2 | 조·항 단위 청킹 및 벡터DB 구축 | ☐ | |
| T3 | 통합 검색 엔진(API) 개발 | ☐ | |
| T4 | 통합 검색 웹 화면(UI) 개발 | ☐ | |
| T5 | 검색 품질 검증 및 운영 체계 마련 | ☑ | 2026-08-31 |
| T5.5 | 웹 화면 디자인 개편 (GNU 브랜드 컬러) | ☑ | 2026-09-01 |
| T6 | RAG 파이프라인 기반 구축 (검색+LLM 연결 엔진) | ☑ | 2026-09-01 |
| T7 | RAG 답변 생성 API 개발 (`/api/ask`) | ☑ | 2026-09-03 |
| T8 | RAG 챗봇 웹 화면(UI) 개발 | ☑ | 2026-09-03 |
| T9 | RAG 품질 검증 및 환각 방지 튜닝 | ☑ | 2026-09-03 |
| T10 | RAG 운영 체계 마련 (로그·비용·장애대응·매뉴얼) | ☑ | 2026-09-03 |

> 📌 본 문서는 각 단계 구현을 **요청하는 프롬프트**를 정리한 것이며, 실제 코드 작성은 각 프롬프트를 실행하는 시점에 진행한다.
> 📌 T6은 2026-09-01, T7~T10은 2026-09-03 **모두 구현 완료**되었다. T1~T10 전체가 완료 상태이다.

